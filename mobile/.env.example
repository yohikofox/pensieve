# Better Auth Configuration
# Story 15.2 — Migration Client Mobile Better Auth
# ADR-029: Better Auth comme provider d'authentification

# Better Auth server URL (self-hosted backend)
EXPO_PUBLIC_BETTER_AUTH_URL=https://api.pensine.example.com

# Backend API Configuration
# For local development, use your Mac's local IP address (e.g., http://10.0.0.35:3000)
# For production, use your deployed backend URL
EXPO_PUBLIC_API_URL=http://docker-host.local:3000

# Note: EXPO_PUBLIC_ prefix makes these variables available in the app
# Copy this file to .env and fill in your actual credentials

# HuggingFace OAuth Configuration
# Create an OAuth app at: https://huggingface.co/settings/connected-applications
# Set redirect URI to your backend URL (e.g., http://10.0.0.35:3000 for local dev)
EXPO_PUBLIC_HF_CLIENT_ID=your-huggingface-client-id
HF_CLIENT_SECRET=your-huggingface-app-secret

# SUPPRIMÉES (Story 15.2 — Migration Better Auth):
# EXPO_PUBLIC_GOOGLE_CLIENT_ID=
