# ==========================================
# Pensine - Environment Configuration
# ==========================================
# Copy this file to .env and fill with your actual values
# NEVER commit .env to version control!

# ==========================================
# NODE ENVIRONMENT
# ==========================================
NODE_ENV=development

# ==========================================
# HOMELAB INFRASTRUCTURE
# ==========================================

# PostgreSQL Database (Application Data)
POSTGRES_PASSWORD=your-secure-postgres-password-here

# RabbitMQ Message Queue
RABBITMQ_PASSWORD=your-secure-rabbitmq-password-here

# MinIO Object Storage (Audio Files)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=your-secure-minio-password-here

# Docker Registry (Container Images)
# Optional: Enable basic auth by creating htpasswd file
# htpasswd -Bc ./registry/auth/htpasswd admin
REGISTRY_URL=http://localhost:5000
REGISTRY_UI_URL=http://localhost:5001

# Container Registry for pulling images
REGISTRY=cregistry.yolo.yt
BACKEND_VERSION=latest
WEB_VERSION=latest

# ==========================================
# GOOGLE OAUTH (Calendar Integration)
# ==========================================
# Get these from: Google Cloud Console → APIs & Services → Credentials
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REDIRECT_URI=https://your-backend-domain/auth/google/callback

# ==========================================
# HUGGINGFACE OAUTH (Gated Models)
# ==========================================
# Required for downloading gated models like Gemma MediaPipe
# Get these from: https://huggingface.co/settings/applications → New Application
# Redirect URI: https://your-backend-domain/auth/huggingface/callback
HF_CLIENT_ID=your-hf-client-id
HF_CLIENT_SECRET=your-hf-client-secret

# ==========================================
# SUPABASE CLOUD (Authentication)
# ==========================================
# Get these values from: https://supabase.com/dashboard → Settings → API

# Supabase Project URL
SUPABASE_URL=https://xxxxx.supabase.co

# Supabase Anon Key (safe for mobile/frontend)
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Supabase Service Role Key (BACKEND ONLY - keep secret!)
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# JWT Secret (for backend token validation)
# Get from: Supabase Dashboard → Settings → API → JWT Settings
JWT_SECRET=your-super-secret-jwt-secret-32-chars-minimum

# ==========================================
# BACKEND API CONFIGURATION
# ==========================================
# (Uncomment when backend is deployed)

# API Server Port
# PORT=3000

# Database Connection URL
# DATABASE_URL=postgres://pensine:${POSTGRES_PASSWORD}@localhost:5432/pensine

# RabbitMQ Connection URL
# RABBITMQ_URL=amqp://pensine:${RABBITMQ_PASSWORD}@localhost:5672

# MinIO Configuration (for presigned URL generation)
# MINIO_ENDPOINT=localhost
# MINIO_PORT=9000
# MINIO_USE_SSL=false
# MINIO_ACCESS_KEY=${MINIO_ROOT_USER}
# MINIO_SECRET_KEY=${MINIO_ROOT_PASSWORD}

# ==========================================
# CLOUDFLARE TUNNEL (Optional - for public access)
# ==========================================
# Tunnel ID (from: cloudflared tunnel create pensine)
# TUNNEL_ID=your-tunnel-id-here

# Tunnel Token (alternative to credentials file)
# TUNNEL_TOKEN=your-tunnel-token-here

# ==========================================
# MOBILE APP CONFIGURATION
# ==========================================
# (These can be set in mobile/.env if needed)

# Deep Linking Scheme
# APP_SCHEME=pensine

# API Base URL (when backend is deployed)
# API_BASE_URL=http://localhost:3000
# API_BASE_URL=https://api.pensine.app (production)

# Storage Base URL (MinIO)
# STORAGE_BASE_URL=http://localhost:9000
# STORAGE_BASE_URL=https://storage.pensine.app (production)
